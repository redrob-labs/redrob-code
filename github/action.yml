name: "Redrob Code GitHub Action"
description: "Run Redrob Code in GitHub Actions workflows"
branding:
  icon: "code"
  color: "orange"

inputs:
  model:
    description: "Model to use"
    required: true

  agent:
    description: "Agent to use. Must be a primary agent. Falls back to default_agent from config or 'build' if not found."
    required: false

  share:
    description: "Share the Redrob Code session (defaults to true for public repos)"
    required: false

  prompt:
    description: "Custom prompt to override the default prompt"
    required: false

  use_github_token:
    description: "Use GITHUB_TOKEN directly instead of Redrob Code App token exchange. When true, skips OIDC and uses the GITHUB_TOKEN env var."
    required: false
    default: "false"

  mentions:
    description: "Comma-separated list of trigger phrases (case-insensitive). Defaults to '/redrob,/rr,/opencode,/oc' -- the upstream phrases are kept for compatibility"
    required: false

  variant:
    description: "Model variant for provider-specific reasoning effort (e.g., high, max, minimal)"
    required: false

  oidc_base_url:
    description: "Base URL for OIDC token exchange API. Only required when running a custom GitHub App install. Defaults to https://api.code.redrob.ai"
    required: false

runs:
  using: "composite"
  steps:
    - name: Get redrob version
      id: version
      shell: bash
      run: |
        VERSION=$(curl -sf https://api.github.com/repos/redrob-labs/redrob-code/releases/latest | grep -o '"tag_name": *"[^"]*"' | cut -d'"' -f4)
        echo "version=${VERSION:-latest}" >> $GITHUB_OUTPUT

    - name: Cache redrob
      id: cache
      uses: actions/cache@v4
      with:
        path: ~/.redrob/bin
        key: redrob-${{ runner.os }}-${{ runner.arch }}-${{ steps.version.outputs.version }}

    - name: Install redrob
      if: steps.cache.outputs.cache-hit != 'true'
      shell: bash
      run: curl -fsSL https://code.redrob.ai/install | bash

    - name: Add redrob to PATH
      shell: bash
      run: echo "$HOME/.redrob/bin" >> $GITHUB_PATH

    - name: Run redrob
      shell: bash
      id: run_redrob
      run: redrob github run
      env:
        MODEL: ${{ inputs.model }}
        AGENT: ${{ inputs.agent }}
        SHARE: ${{ inputs.share }}
        PROMPT: ${{ inputs.prompt }}
        USE_GITHUB_TOKEN: ${{ inputs.use_github_token }}
        MENTIONS: ${{ inputs.mentions }}
        VARIANT: ${{ inputs.variant }}
        OIDC_BASE_URL: ${{ inputs.oidc_base_url }}
